kind: pipeline
type: docker
name: CICD

trigger:
  event:
    - push

steps:
  - name: build
    image: gradle
    commands:
      - cd src
      - chmod +x gradlew
      - ./gradlew build
    when:
      branch:
      - master